<h2>Edit Contact</h2>

<%= render "form.html", changeset: @changeset,
                        contact_lots: @contact.lots,
                        owned_lots: @contact.lots_owned,
                        action: contact_path(@conn, :update, @contact),
                        lots: @lots %>

<span><%= link "Back", to: contact_path(@conn, :index) %></span>
