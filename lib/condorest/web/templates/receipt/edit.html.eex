<h2>Edit Receipt</h2>

<%= render "form.html", changeset: @changeset,
                        action: receipt_path(@conn, :update, @receipt) %>

<span><%= link "Back", to: receipt_path(@conn, :index) %></span>
